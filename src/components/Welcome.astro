---
import AlpacaMarketApi from "./AlpacaMarketApi";
import CarouselView from "./ui/carousel-view";
import '../styles/global.css'

// const socket: WebSocket = new WebSocket(
//     `wss://stream.data.sandbox.alpaca.markets/v2/test`,
// );
// socket.onopen = () => {
//     socket.send(
//         JSON.stringify({
//             action: "auth",
//             key: `${KEY_ID}`,
//             secret: `${SECRET}`,
//         }),
//     );
// };

// socket.onmessage = (event) => {
//     console.log(event.data);
//     JSON.stringify({
//         action: "subscribe",
//         trades: ["AAPL", "FAKEPACA"],
//     });
// };

// socket.close
// socket.onclose = (event) => {
//     console.log("Socket closed", event);
// };
---

<AlpacaMarketApi client:only />
<CarouselView client:idle/>
